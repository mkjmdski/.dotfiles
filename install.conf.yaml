- defaults:
    link:
      create: true
      relink: true
      force: true
    shell:
      stdout: true
- create:
  - "$HOME/bin"
  - "$HOME/.terraform-plugins"
- shell:
  - command: bash packets.sh
    stdin: true
  - chsh -s $(which zsh)
  - pip3 install --user --upgrade -r requierements.txt
  - git config --global include.path "$PWD/git/global.gitconfig"
  - git config --global core.excludesfile "$PWD/git/global.gitignore"
  - for extension in $(cat vscode/installed_vs_extensions); do code --install-extension $extension; done
- link:
    ~/.config/nvim/colors/molokai.vim:
      path: vim/molokai.vim
    ~/.config/nvim/init.vim:
      path: vim/.vimrc
    ~/.zshrc:
      path: zsh/.zshrc
    ~/.config/Code/User/settings.json:
      path: vscode/settings.json
      if: uname -a | grep -iq ubuntu || uname -a | grep -iq arch
    ~/.config/Code/User/snippets:
      path: vscode/snippets
      if: uname -a | grep -iq ubuntu || uname -a | grep -iq arch
    ~/.config/Code - OSS/User/settings.json:
      path: vscode/settings.json
      if: uname -a | grep -iq manjaro
    ~/.config/Code - OSS/User/snippets:
      path: vscode/snippets
      if: uname -a | grep -iq manjaro
    ~/Library/Application Support/Code/User/settings.json:
      path: vscode/settings.json
      if: uname -a | grep -iq darwin
    ~/.icons:
      path: gnome/icons
      if: gnome-shell --version
    ~/.themes:
      path: gnome/themes
      if: gnome-shell --version
    ~/.local/share/gnome-shell/extensions:
      path: gnome/extensions
      if: gnome-shell --version
    ~/.config/terminator/config:
      path: terminator.config
      if: terminator --version
- shell:
  - zsh -ic "zplug load; zplug install"
